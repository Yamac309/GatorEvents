# Leave empty for local dev (Vite proxies /api to localhost:3001).
# In production (Vercel), set this to your deployed backend origin, no trailing slash:
# VITE_API_URL=https://gatorevents-api.onrender.com
VITE_API_URL=
