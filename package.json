{
  "name": "gatorevents",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered event discovery for UF students & Gainesville locals — full-stack portfolio project (React + Express + Supabase + Gemini).",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently --kill-others --names \"SERVER,CLIENT\" --prefix-colors \"blue,magenta\" \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
